import 'package:flutter/material.dart';

class BookWithNotesPage extends StatelessWidget {

  const BookWithNotesPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}